/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Muda disto: "tailwindcss": {},
    // Para isto:
    '@tailwindcss/postcss': {}, 
  },
};

export default config;